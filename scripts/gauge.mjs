#!/usr/bin/env node
// 轉接：引擎本體放在 skill 資料夾裡（.claude/skills/skill-gauge/scripts/gauge.mjs），
// 這樣把 skill 資料夾複製到 ~/.claude/skills/ 也帶得走引擎。這裡只是方便從 repo 根目錄執行。
import './../.claude/skills/skill-gauge/scripts/gauge.mjs';
