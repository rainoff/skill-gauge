# Results 模板 — 跑完先填數據，再寫結論

> 順序就是紀律：§1–§6 填完之前，不寫任何一句結論。
> 判準以 pre-registration 為準；跑的過程發現判準有灰區，記在 §7，不回頭改判準。

## 1. 實際執行的組態

| 欄位 | 值 |
|------|-----|
| 執行模型（executor） | {實跑值＋佐證方式（執行紀錄、API log）} |
| 評分模型（analyzer/judge） | {實跑值；一位判整組？還是一份一位？後者要記錄標準漂移風險} |
| 規模 | {題 × 配置 × run；invalid 幾次、怎麼補跑} |
| 座標宣告 | {照 pre-registration 的四座標表逐欄回填實際值——量測前宣告的，量測後對帳} |

## 2. 開跑前決定並記錄的處置（不涉判準修改）

{材料處理、操作化細節（例如「不橫向捲動」用什麼視窗寬度量）——凡 pre-registration 沒指定、執行時才定的，逐項記錄}

## 3. Gate（不進分）

{通過數；invalid 清單（量到什麼值、怎麼處置）；原輸出保留位置}

## 4. 條件完整性（從執行紀錄實查）

| 檢查項 | 結果 |
|--------|------|
| with_skill 臂是否真的載入 skill | {n/n} |
| 對照臂是否全程未碰 skill（未呼叫、未讀檔、不在可用清單） | {n/n} |
| 有沒有意外破壞隔離的事件（偷派 subagent、外部呼叫） | {…} |

## 5. 逐項通過數

{每題一表：assertion × 兩臂，分母寫明。總和給合併計數；若另有逐 run 平均通過率，兩個數字都標明算法——同一份資料兩種算法會出兩個不同的數字，引用時必須講清楚是哪一種}

## 6. 成本分佈

{兩臂的 output tokens 與時長：中位數＋範圍。記帳異常逐格排除並說明}

## 7. 判準灰區（跑的過程發現的，跟著結論走）

{哪幾格的判定取決於判準的某種解讀？換一種解讀，結論會不會翻？——灰區清單必須跟著任何引用一起走，摘要層把灰區壓掉＝失真}

## 8. 取向觀察（不進分，強制記錄）

{二元判準抓不到、但真實存在的行為差異。這裡常常藏著比計分表更重要的東西}

## 9. 這次 run 能說與不能說的話

### 能說（描述性，僅限本次 run）

> {一段完整的話，正反差異都在同一句裡。**引用規則：整句帶走，不可只截一半**}

### 不能說（逐條，附理由）

1. {…}

### 限制（最要緊的先寫）

{對照臂實際拿到什麼？量到的是完整效果還是殘餘差值？兩臂有沒有捆綁變因無法分解？——照實寫。量測條件與樂觀偏差寫在結果旁，是這套方法對「隔離的誠實」的承諾}

（roadmap 預留：敏感度句式——「隱藏偏誤要多大才會翻轉此結論」，見 [docs/roadmap.md](../docs/roadmap.md)）

## 10. 天花板檢查

{有沒有整題兩臂全過、零鑑別？非通過格是否集中在單一習慣上（有效差異比表面計數小）？run 之間是不是近乎複製品（有效樣本比 n 小）？}

## 11. 下一版候選（對判準本身的批評，未裁）

{評分過程中對判準的批評逐條留存——這是下一版的題目來源}
